"use client";

import { AuthCard } from "@/page-component/auth/AuthForm";
import { AuthPageContainer } from "@/page-component/auth/Auth.styled";

export default function AuthPage() {
  return (
    <AuthPageContainer>
      <AuthCard />
    </AuthPageContainer>
  );
}
